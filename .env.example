# Variables de entorno requeridas.
# Copia este archivo como .env.local y completa los valores reales.
# NUNCA commitees .env.local — contiene secrets reales.

# Token de read/write del store de Vercel Blob usado por el relay de claims QR
# (/api/sync/claim). Requerido solo en runtime (dev/prod); el build la ignora.
BLOB_READ_WRITE_TOKEN=